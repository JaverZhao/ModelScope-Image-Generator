# 产品需求文档 (PRD): ModelScope AI 绘图助手
## 1. 产品概述
- 项目名称：ModelScope Image Generator (MS-Gen)
- 产品定位：一款基于魔搭API的轻量级桌面/Web端AI绘图工具。
- 核心价值：解决开发者或设计师在使用魔搭API时缺乏图形化界面的痛点，提供从API配置、参数调整到历史记录管理的全流程解决方案。

## 2. 功能需求
### 2.1 API 配置模块
- 功能描述：用户可以输入自己的 ModelScope API Key。
- 交互：提供一个密码输入框（输入时显示为掩码）。
- 逻辑：API Key 应在本地持久化存储（如保存到本地文件），无需每次重启应用都重新输
- 验证：提供一个“测试连接”按钮，验证 Key 是否有效。
### 2.2 参数控制区 (左侧面板)
- Prompt (提示词)：大文本输入框，支持换行。
- Negative Prompt (负面提示词)：可选，文本输入框。
- Model Selection (模型选择)：下拉菜单，默认为 Tongyi-MAI/Z-Image-Turbo，预留其他模型选项，支持手动输入模型ID。
- Image Size (尺寸)：下拉菜单 (1024x1024, 720x1280 等)。
- Advanced Settings (折叠面板)：
    - Steps (步数)：滑块，范围 10-50，默认 20。
    - Guidance Scale (引导系数)：滑块，范围 1-10，默认 3。
    - Seed (种子)：输入框，默认为 -1 (随机)，若指定具体数字则复现结果。
- 生图按钮：点击后触发请求，按钮变为 Loading 状态，底部显示当前任务进度。
### 2.3 历史生图区 (右侧主区域)
- 布局：采用瀑布流或网格布局展示生成的图片缩略图。
- 信息展示：每张缩略图下方显示简化的 Prompt 前缀和生成时间。
- 交互：
    - 点击图片：弹窗显示大图，并提供下载按钮。
    - 删除：提供删除单个图片的功能。
### 2.4 反馈机制
- 状态轮询：生图过程中显示当前状态（如：任务提交中 -> 排队中 -> 生成中 -> 完成）。
- 错误处理：API Key 错误、余额不足、内容违规等错误需通过 Toast 或弹窗明确提示用户。
## 3. 技术栈方案
示例代码为 Python，推荐以下全 Python 技术栈，开发效率最高：

- 开发语言：Python 3.8+
- 前端/界面框架：Streamlit (极速构建数据应用，自带状态管理、网格布局、弹窗组件)
- HTTP 客户端：requests (用于 API 通信)
- 图像处理：Pillow (PIL, 用于处理图片流)
- 数据持久化：保存本地 JSON 文件 (用于保存历史记录和配置)